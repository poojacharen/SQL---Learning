-- Viewing Triggers - using SHOW TRIGGERS we can view all the triggers from the current database

SHOW TRIGGERS LIKE 'payments%'   -- We can use the conventions to see the triggers

-- we add the tablename, before or after and finally the type of the SQL statement which can be insert, update or delete
--    tablename_after_insert
